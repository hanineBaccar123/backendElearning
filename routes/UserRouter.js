var express = require('express');
var router = express.Router();
const UserController = require("../Controllers/UserController")

/* GET home page. */
//router.get('/', UserActivation.get);

module.exports = router;
